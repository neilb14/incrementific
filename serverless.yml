service: incrementific

provider:
  name: aws
  runtime: nodejs12.x

functions:
  register:
    handler: register.handler
    events:
      - http:
          path: register
          method: post
  hello:
    handler: handler.hello

package:
  individually: true